# Models Directory

Use this directory for:

- downloaded model weights
- exported ONNX/TensorRT models
- experiment checkpoints you want to keep

Do not commit large binary weights unless you intentionally want them in Git.

